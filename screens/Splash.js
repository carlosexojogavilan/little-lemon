import { SafeAreaView, Text } from "react-native";

const Splash = () => {
  return (
    <SafeAreaView>
      <Text>Splash Screen</Text>
    </SafeAreaView>
  );
};

export default Splash;
